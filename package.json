{
  "name": "@tillkit/root",
  "private": true,
  "version": "0.0.1",
  "description": "TillKit - Open source e-commerce starter kit",
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm recursive run build",
    "dev": "pnpm --filter ./templates/starter dev",
    "test": "pnpm recursive run test",
    "test:watch": "pnpm --filter @tillkit/core test:watch",
    "lint": "eslint packages templates --ext .ts,.js",
    "lint:fix": "eslint packages templates --ext .ts,.js --fix",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "create-tillkit": "node packages/create-tillkit/bin/create-tillkit.js"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.0",
    "prettier": "^3.1.1",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}